type Mutation {
  register(email: String!, password: String!): User!
  login(email: String!, password: String!): User!
  logout: Boolean
  forgotPassword(email: String!): Boolean!
  resetPassword(newPassword: String!, key: String!): Boolean!
}
